require 'test_helper'

class MedalTest < ActiveSupport::TestCase
  # test "the truth" do
  #   assert true
  # end

 #  test "should be valid" do
 #   assert @medal.valid?
    #### Medal curently has no attributes, so there's nothing(no attributes) to test if valid or not.
 # end
end
